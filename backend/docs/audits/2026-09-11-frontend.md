# Audit frontend — 11/09/2026

Phạm vi toàn bộ `frontend/frontend/src`, tập trung Booking, payment, auth và dashboard mega-components. Dòng tính theo source sau sửa.

## 🔴 Critical — lỗi runtime/type đã sửa

**File**: `components/dashboard/AdminHeader.tsx` dòng 1; `components/dashboard/ShowtimePlannerWizard.tsx` dòng 970–975; `components/dashboard/ShowtimeManagementPage.tsx`.

`AlertTriangle` chưa import gây crash khi render nhánh cảnh báo. Wizard gọi `autoDistributeCapacity` không tồn tại. Một legacy modal hơn 700 dòng không có caller gây type errors và tăng tải file. Đã import icon, gọi lại flow `requestPreview(false, true)` và tách `ShowtimeSeatsLayoutModal.tsx`/loại bỏ legacy code không được dùng.

## 🟡 Medium — trạng thái bất đồng bộ và payment URL

**File**: `utils/authSession.ts` dòng 18–27; `components/auth/LoginForm.tsx` dòng 207–243; `hooks/useLatestRequest.ts`; `components/dashboard/MovieManagementPage.tsx`, `ShowtimeManagementPage.tsx`, `CustomerContactManagementPage.tsx`.

`setAuthToken` giờ phát event ngay sau khi ghi localStorage. Login điều hướng ngay sau token, còn avatar fetch chỉ ghi nếu token hiện tại vẫn là token login; request list dùng sequence guard để response cũ không ghi đè response mới. Contact timer và effect fetch có cleanup.

```ts
const isCurrent = startRequest();
const data = await movieService.getMovies(params);
if (!isCurrent()) return;
setMovies(data.content);
```

**File**: `utils/paymentLinks.ts`; `components/dashboard/StaffTicketSalesPage.tsx` dòng 1015–1020, 1154–1159.

MoMo có thể không trả QR/deeplink; helper dùng `payUrl` làm fallback. Polling thanh toán tránh request chồng nhau và dừng khi modal unmount.

## 🟢 Low — error handling và bundle

`api/errors.ts` xử lý null/generic error và giữ fallback; `promotionApi.ts` bổ sung trường wallet method; component promotions dùng đúng response type. Không tìm thấy `dangerouslySetInnerHTML`, `innerHTML`, `eval` hay `console.log` trong quét source hiện tại (các `console.error` còn lại là logging lỗi best-effort).

Các route chính đã lazy-load. Bundle sau build gồm chunk dashboard lớn nhất khoảng 128 kB gzip khoảng 33 kB cho ShowtimeManagementPage; file source 100 kB không đồng nghĩa bundle 100 kB. Nên tiếp tục tách `ShowtimeManagementPage` theo query/data/modal, `MovieManagementPage` theo search/editor, `StaffTicketSalesPage` theo cart/payment, `ComboManagementPage` theo editor/table, `BookingPage` theo seat/combo/payment; đưa fetch vào hooks và reducer cho wizard. Chưa thêm Redux/Zustand vì chưa có bằng chứng cần đổi stack.

## Kiểm tra

- `npm run typecheck`: pass.
- `npm run lint`: pass, không error hoặc warning.
- `npm run build`: pass, Vite 8.0.16.
- `npm test -- --run`: **5 tests pass** cho auth event, error fallback, payment union và stale request.

Không chạy browser E2E/WebSocket thật; cần kiểm tra trên môi trường backend chạy và qua Nginx sau khi Docker daemon khả dụng.
